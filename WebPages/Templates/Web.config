<?xml version="1.0"?>
<configuration>

  <system.web>
    <!-- Deny everyone direct access to the template folder from the web -->
    <authorization>
      <deny users="*"/>
      <deny users="?"/>
    </authorization>
  </system.web>

</configuration>
